import * as React from 'react';
import HostComponent from "../components/HostComponent";

function Host({navigation}) {
  return (
      <HostComponent navigation={navigation}/>
  );
}
export default Host;
